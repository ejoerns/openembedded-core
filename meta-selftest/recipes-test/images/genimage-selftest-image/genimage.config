image ${GENIMAGE_IMAGE_FULLNAME} {
    hdimage {
        align = 1M
        partition-table-type = gpt
    }

    partition rootfs {
        image = "core-image-minimal${IMAGE_MACHINE_SUFFIX}.rootfs.ext4"
        size = 256M
    }
}
